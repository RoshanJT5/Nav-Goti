import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.navgoti.game',
    appName: 'Nav Goti',
    webDir: 'out',
    server: {
        androidScheme: 'https'
    }
};

export default config;
